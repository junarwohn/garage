#include <iostream>

int str_len(char * str_) {
	char iter = * str_;
	int len_ = 0;
	while (

void char * ppap_scan(char * str_){
	char iter = * str_;

}

int main(void) {
	char input[1000001];
	std::cin >> input;
}
