#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	//cin.tie(0);
	cout << "Input the base" << '\n';
	int base;
	cin >> base;
	string input_str;
	cin >> input_str;
	cout << input_str;
	return 0;
}

