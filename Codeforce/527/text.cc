#include <iostream>
#include <string>
#include <algorithm>

using namespace std;
	

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	/*
	int n;
	cin >> n;
	string * input_list = new string[2 * n - 2];
	for(int i = 0; i < 2 * n - 2; i++) {
		cin >> input_list[i];
		input_list[i][2] = 'k';
		cout << input_list[i] << '\n';
	}
	*/
	string t;
	t.resize(10, '1');
	cout << t;
	
	
	return 0;
}

